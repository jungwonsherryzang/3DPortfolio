import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

//need to pass the ID of the element that we want to connect to
ReactDOM.createRoot(document.getElementById('root')).render(<App />);